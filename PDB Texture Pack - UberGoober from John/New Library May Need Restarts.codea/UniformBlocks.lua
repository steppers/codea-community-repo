function uniformBlocks()
        local dirt = scene.voxels.blocks:new("Dirt")
    dirt.setTexture(ALL, packPrefix.."Dirt")

local dirt = scene.voxels.blocks:new("Gravel Dirt")
dirt.setTexture(ALL, packPrefix.."Gravel Dirt")
    
local stone = scene.voxels.blocks:new("Stone")
stone.setTexture(ALL, packPrefix.."Stone")

local sand = scene.voxels.blocks:new("Gravel Stone")
sand.setTexture(ALL, packPrefix.."Gravel Stone")

local sand = scene.voxels.blocks:new("Greystone")
sand.setTexture(ALL, packPrefix.."Greystone")

local sand = scene.voxels.blocks:new("Redstone")
sand.setTexture(ALL, packPrefix.."Redstone")

local sand = scene.voxels.blocks:new("Sand")
sand.setTexture(ALL, packPrefix.."Sand")

local sand = scene.voxels.blocks:new("Greysand")
sand.setTexture(ALL, packPrefix.."Greysand")

local sand = scene.voxels.blocks:new("Redsand")
sand.setTexture(ALL, packPrefix.."Redsand")

local sand = scene.voxels.blocks:new("Snow")
sand.setTexture(ALL, packPrefix.."Snow")

local sand = scene.voxels.blocks:new("Ice")
sand.setTexture(ALL, packPrefix.."Ice")

local sand = scene.voxels.blocks:new("Lava")
sand.setTexture(ALL, packPrefix.."Lava")

    local brickRed = scene.voxels.blocks:new("Red Brick")
    brickRed.setTexture(ALL, packPrefix.."Brick Red")
    
    local brick = scene.voxels.blocks:new("Brick")
    brick.setTexture(ALL, packPrefix.."Brick Grey")
    
    local coalOre = scene.voxels.blocks:new("Coal Ore")
    coalOre.setTexture(ALL, packPrefix.."Stone Coal")

local coalOre = scene.voxels.blocks:new("Iron Ore")
coalOre.setTexture(ALL, packPrefix.."Stone Iron")

local coalOre = scene.voxels.blocks:new("Silver Ore")
coalOre.setTexture(ALL, packPrefix.."Stone Silver")
    
    local goldOre = scene.voxels.blocks:new("Gold Ore")
    goldOre.setTexture(ALL, packPrefix.."Stone Gold")
    
    local diamondOre = scene.voxels.blocks:new("Diamond Ore")
    diamondOre.setTexture(ALL, packPrefix.."Stone Diamond")
    
    local redstoneOre = scene.voxels.blocks:new("Emerald Ore")
    redstoneOre.setTexture(ALL,packPrefix.."Redstone Emerald")   

local redstoneOre = scene.voxels.blocks:new("Ruby Ore")
redstoneOre.setTexture(ALL,packPrefix.."Greystone Ruby")   
    
    local planks = scene.voxels.blocks:new("Planks")
    planks.setTexture(ALL, packPrefix.."Wood")

local planks = scene.voxels.blocks:new("Red Planks")
planks.setTexture(ALL, packPrefix.."Wood Red")
    
end